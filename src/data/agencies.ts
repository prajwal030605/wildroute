import { Agency } from "@/types";

// Static agency data removed — all agencies are now loaded live from Supabase.
// This file is kept as an empty array so existing imports do not break.
export const agencies: Agency[] = [];
