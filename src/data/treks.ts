import { Trek } from "@/types";

// Static trek data removed — all treks are now loaded live from Supabase.
// This file is kept as an empty array so existing imports do not break.
export const treks: Trek[] = [];
